# Netlify 上傳文件指南

## 📦 需要上傳的文件

### 方法 1：通過 Git（推薦，最簡單）

**您只需要：**
1. 確保所有必需文件都在 Git 倉庫中
2. 提交並推送代碼
3. 在 Netlify 連接 Git 倉庫

**Netlify 會自動：**
- 只部署需要的文件（`.gitignore` 會自動排除不需要的文件）
- 執行構建命令
- 部署 Function

### 方法 2：手動上傳（不推薦，但可以）

如果手動上傳，需要上傳以下**文件結構**：

```
您的項目/
├── netlify.toml              ← 必需！根目錄的配置文件
├── index_netlify.html        ← 必需！前端頁面
└── netlify/                  ← 必需！Function 資料夾
    └── functions/
        ├── calculate_hd/
        │   └── __init__.py   ← 必需！Function 代碼
        └── requirements.txt  ← 必需！Python 依賴
```

## ❌ 不需要上傳的文件

以下文件**不需要**上傳（`.gitignore` 已自動排除）：

- ❌ `app.py` - Flask 伺服器（Netlify 不使用）
- ❌ `app_enhanced.py` - Flask 伺服器
- ❌ `test_*.py` - 測試文件
- ❌ `example_*.py` - 示例文件
- ❌ `*.md` - 文檔文件（可選，如果想保留可以保留）
- ❌ `ephe/` - 大型數據文件（可選）
- ❌ `requirements.txt` - Flask 依賴（Netlify Function 有自己的）

## 🎯 最簡單的方法

### 通過 Git 部署（推薦）

1. **確認文件已存在**：
   - ✅ `netlify.toml`（根目錄）
   - ✅ `index_netlify.html`（根目錄）
   - ✅ `netlify/functions/calculate_hd/__init__.py`
   - ✅ `netlify/functions/requirements.txt`
   - ✅ `.gitignore`（已配置好）

2. **提交到 Git**：
   ```bash
   git add .
   git commit -m "Prepare for Netlify deployment"
   git push
   ```

3. **在 Netlify 連接 Git 倉庫**：
   - 登入 Netlify
   - 「Add new site」→「Import an existing project」
   - 選擇您的 Git 倉庫
   - Netlify 會自動部署

**就是這麼簡單！** Netlify 會自動：
- 讀取 `netlify.toml` 配置
- 執行構建命令（複製 `index_netlify.html` 為 `index.html`）
- 部署 Function
- 發布網站

## 📋 檢查清單

部署前確認：

- [ ] `netlify.toml` 在根目錄
- [ ] `index_netlify.html` 在根目錄
- [ ] `netlify/functions/calculate_hd/__init__.py` 存在
- [ ] `netlify/functions/requirements.txt` 存在
- [ ] `.gitignore` 已配置（已為您配置好）
- [ ] 所有文件已提交到 Git

## 💡 重要提示

**您不需要：**
- ❌ 手動選擇要上傳的文件
- ❌ 只上傳 `netlify/` 資料夾
- ❌ 擔心上傳太多文件（`.gitignore` 會自動排除）

**您只需要：**
- ✅ 確保必需文件在 Git 倉庫中
- ✅ 提交並推送代碼
- ✅ 在 Netlify 連接 Git 倉庫

就是這麼簡單！

