# 部署前檢查清單

對照本機 `git_push.bat` 流程：推送前確認下列項目，可減少 Vercel／LINE Webhook 部署後才發現的問題。

---

## 一、推送 GitHub 前（本機）

1. **程式可啟動**：在本機執行 Flask，確認首頁與主要功能正常。
2. **依賴完整**：`requirements.txt` 已包含本次變更需要的套件（例如 `line-bot-sdk`）。
3. **敏感資訊不要進 Git**：Channel Secret、Access Token 只放在 Vercel 環境變數，不要寫進程式碼或 `.env` 若該檔會被提交。

---

## 二、Vercel 專案設定（推送後仍須檢查）

1. **環境變數**（Settings → Environment Variables），依需求設定：
   - `LINE_CHANNEL_SECRET`、`LINE_CHANNEL_ACCESS_TOKEN`（LINE Messaging API）
   - `LINE_DEFAULT_TIMEZONE` 或 `DEFAULT_TIMEZONE`（例如 `Asia/Taipei`，供計算出生時刻）
   - 若使用資料庫：`DATABASE_URL` 等（與現有說明一致）
2. **變更環境變數後**：在 Vercel 對該部署執行 **Redeploy**，否則舊部署仍用舊變數。
3. **部署成功**：在 Vercel Dashboard 確認最新 commit 已 **Ready**，且 Build Log 無錯誤。

---

## 三、推送後驗證（建議依序做）

1. **Webhook 網址是否為新版程式**  
   瀏覽器開啟（GET）下列任一網址（應為 HTTP **200** 與簡短英文說明）：
   - `https://你的網域/callback`
   - `https://你的網域/api/line/callback`（備用路徑，與 `/callback` 行為相同）

   若兩者皆為 **404**（Flask 預設「Not Found」頁）：
   - 到 Vercel → **Deployments** → 點選**正式環境**最新一筆 → 確認 **Commit** 是否為你本機已含 `line_callback` 的版本（本機可執行 `git rev-parse HEAD` 比對）。
   - 若 Commit 較舊：請 `git push` 後等待部署完成，或手動 **Redeploy**（必要時勾選 **Clear build cache**）。
   - 確認 Vercel 專案連到的 **GitHub 儲存庫／分支** 與你推送的專案一致（專案設定 → **Git**）。

   若 **`/health` 可 200** 但 **`/callback` 仍 404**：請推送含備用路徑的版本後，再試 **`/api/line/callback`**；若備用可通、主路徑不通，請將 LINE Webhook 暫改為 `https://你的網域/api/line/callback` 亦可。

2. **LINE Developers**  
   Webhook URL 填下列其一（與程式路由一致即可）：
   - `https://你的網域/callback`
   - `https://你的網域/api/line/callback`（若主路徑有異常可改用此備用網址）  
   開啟 **Use webhook** 後按 **Verify**。預期：**200**；若 **400 Invalid signature**，請核對 `LINE_CHANNEL_SECRET` 是否與該 Channel 一致。

3. **本機推送流程**（與 `git_push.bat` 一致）  
   - `[1/4]` `git status` 確認要提交的檔案  
   - `[2/4]` `git add .`  
   - `[3/4]` `git commit -m "你的說明"`  
   - `[4/4]` `git push`  
   若 push 失敗，檢查 `git remote -v`、權限與網路。

---

## 四、使用 `git_push.bat` 時

1. 在專案目錄執行批次檔，依提示輸入提交訊息（可直接 Enter 使用預設訊息，建議改成與本次修改相符的說明）。  
2. 推送完成後，到 Vercel 確認自動部署已觸發並成功，再執行「三、推送後驗證」。

---

### 若 Vercel 部署顯示 **Error**（Build Failed）

1. 在失敗的那一筆部署點進去，查看 **Building** 或 **Logs** 裡的紅字（常見為 `pip install` 失敗）。
2. **Python 3.12 與 line-bot-sdk**：本專案 `requirements.txt` 已使用 `line-bot-sdk>=3.13` 系列，以相容 Vercel 預設的 Python 3.12。若你本機仍為 **Python 3.7**，請改用 **3.8 以上** 虛擬環境安裝依賴，否則本機 `pip install` 可能與 Vercel 解析結果不同。
3. 修正後請 **Redeploy**；必要時勾選 **Clear build cache** 再部署一次。
