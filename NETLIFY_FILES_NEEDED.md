# Netlify 部署需要的文件

## 📁 必需的文件

Netlify 部署只需要以下文件：

### 1. 配置文件
- `netlify.toml` - Netlify 配置文件（必需）

### 2. 前端文件
- `index_netlify.html` - Netlify 版本的前端頁面（必需）
- `index.html` - 會在構建時自動生成（不需要手動上傳）

### 3. Netlify Function 文件
```
netlify/
  functions/
    calculate_hd/
      __init__.py          # Function 主文件（必需）
    requirements.txt       # Python 依賴（必需，即使為空）
```

### 4. 可選文件
- `.gitignore` - Git 忽略文件（建議）
- `README.md` - 項目說明（可選）

## ❌ 不需要的文件

以下文件**不需要**上傳到 Netlify：

### Flask 相關（Netlify 不使用 Flask）
- `app.py`
- `app_enhanced.py`
- `requirements.txt`（Flask 版本的）
- `start_server.bat`
- `start_server.sh`

### 測試和示例文件
- `test_*.py`
- `example_*.py`
- `quick_test.py`
- `human_design_example.py`

### 開發工具
- `快速啟動.bat`
- `啟動伺服器.bat`
- `運行人類圖計算器.bat`
- `run_test.bat`

### 大型數據文件（可選）
- `ephe/` 資料夾（星曆文件，通常很大）

### 文檔文件（可選，如果想保留可以保留）
- `*.md` 文件（除了 README.md）
- 其他說明文件

## 📦 部署方式

### 方法 1：通過 Git（推薦）

1. **確保 `.gitignore` 已正確設置**（已為您配置）
2. **提交必要文件到 Git**：
   ```bash
   git add netlify.toml
   git add index_netlify.html
   git add netlify/
   git add .gitignore
   git commit -m "Prepare for Netlify deployment"
   git push
   ```

3. **在 Netlify 中連接 Git 倉庫**：
   - Netlify 會自動檢測並只部署已提交的文件
   - `.gitignore` 中的文件會被自動排除

### 方法 2：手動上傳（不推薦）

如果要手動上傳，只需要上傳：
- `netlify.toml`
- `index_netlify.html`
- `netlify/` 資料夾（包含 functions）

## ✅ 檢查清單

部署前確認：

- [ ] `netlify.toml` 存在
- [ ] `index_netlify.html` 存在
- [ ] `netlify/functions/calculate_hd/__init__.py` 存在
- [ ] `netlify/functions/requirements.txt` 存在
- [ ] `.gitignore` 已配置（排除不需要的文件）
- [ ] 所有必需文件已提交到 Git
- [ ] 構建命令已正確設置

## 💡 提示

1. **使用 `.gitignore`**：已經為您配置好了，會自動排除不需要的文件
2. **Git 方式最簡單**：連接 Git 倉庫後，Netlify 會自動部署，每次推送都會觸發部署
3. **文件大小**：如果 `ephe/` 資料夾很大，建議在 `.gitignore` 中排除它（已經註釋掉了，如果需要可以取消註釋）

## 🔍 驗證文件結構

部署後，Netlify 的發布目錄應該只包含：

```
.
├── netlify.toml
├── index.html              (構建時自動生成)
├── index_netlify.html
└── netlify/
    └── functions/
        ├── calculate_hd/
        │   └── __init__.py
        └── requirements.txt
```

這就是 Netlify 需要的所有文件！

